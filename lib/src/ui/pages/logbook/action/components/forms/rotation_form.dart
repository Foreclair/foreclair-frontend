import 'package:flutter/material.dart';

class RotationForm extends StatelessWidget {
  const RotationForm({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
