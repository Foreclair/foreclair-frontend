import 'package:flutter/material.dart';

class InterventionForm extends StatelessWidget {
  const InterventionForm({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
