import 'package:flutter/material.dart';

class HealForm extends StatelessWidget {
  const HealForm({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
