import 'package:flutter/material.dart';

class SupervisionForm extends StatelessWidget {
  const SupervisionForm({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
